# Task 20.2 — Logs & usage viewer

## Steps

- [x] `GET /usage/events` — recent usage_events (filter by type)
- [x] `GET /usage/jobs` — recent jobs (filter failed for errors)
- [x] Enhance `/app/usage` with recent events + jobs tables
- [x] Light tests + docs (`docs/governance.md`, `docs/portal.md`)

## Acceptance criteria

- [x] Org admin can view mentions, token usage, jobs, and errors (OR-07)
- [x] Cross-tenant list denied
- [x] Aggregates (summary/budgets) still available on the same page
