# Task 1.4 — Health stubs

## Steps

- [x] Add FastAPI app with `GET /health` returning 200 JSON
- [x] Replace Next.js home with a simple “OK” page
- [x] Document tunnel plan (cloudflared/ngrok) for Slack Events later
- [x] Light smoke: uvicorn `/health` and `next` page load / build

## Acceptance criteria

- [x] `GET /health` returns HTTP 200 (shallow stub; deep checks in Sprint 2)
- [x] Next.js serves an “OK” page
- [x] Tunnel plan documented for later sprints
- [x] `uvicorn` and `next` can start
